# Project Configuration
project_name = "vibes"
location     = "Australia East"

# Azure Subscription Configuration
subscription_id   = "f9dc50e2-b88a-4c20-b3ad-0c6add93a139"  # Your current dev subscription
subscription_name = "your-subscription-name"
tenant_id        = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Azure DevOps Configuration
azuredevops_org_service_url = "https://dev.azure.com/your-org-name"

# Git Repository Configuration
# Option 1: Azure Repos (created in the project)
git_repository_url = "https://dev.azure.com/your-org-name/vibes/_git/vibes"
git_username       = "your-email@domain.com"

# Option 2: External Git (GitHub, GitLab, etc.)
# git_repository_url = "https://github.com/spik3r/iac.git"
# git_username       = "spik3r"

# Docker Configuration
docker_image_name = "vibes-app"

# Dev Environment Configuration (from your existing infrastructure)
dev_resource_group_name      = "vibes-dev-rg"
dev_container_registry_name  = "vibesacrdev"
dev_app_service_name         = "vibes-dev-app"

# Pipeline Configuration
create_pipelines = true

# Tags
tags = {
  Environment = "dev"
  Project     = "vibes"
  ManagedBy   = "terraform"
  Purpose     = "pipeline-mvp"
}